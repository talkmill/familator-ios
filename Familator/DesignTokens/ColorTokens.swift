import SwiftUI

extension Color {
    static let errorForeground = Color.red
    static let successForeground = Color.green

    static let toastBackground = Color(.systemBackground)
    static let toastShadow = Color.black.opacity(0.15)
}
